# MiaoMenu_fork — 更新日誌 / Changelog

> 完整版本歷程；對外簡介請看 [README.md](./README.md) ／ English overview at [docs/README-en.md](./docs/README-en.md)。
>
> 想看「為什麼這樣改」與檔案級別差異，請看本機保留的 `README.AI.md`（給 AI 接續上下文用，未推上遠端）。

雙語對照：[繁體中文](#繁體中文) ｜ [English](#english)

---

## 繁體中文

### `1.2` 增訂續（2026-06-20，DeluxeMenus 自動轉換匯入 + 補翻譯 + 作者標 Avery）

> Avery 看了 1.2 後三項加碼：「DeluxeMenus 的匯入要支援」、「有些幫助頁面根本沒有翻譯到中文」、「作者部分只要寫我，原著 github 裡寫了」。讀過真實 DeluxeMenus 資料夾後，把先前排除掉的 DeluxeMenus/dmenu 加回 import 候選，並做真正的 schema 轉換（而非整批複製）。本段為 1.2 的後續增訂，使用者既有指令／別名／權限／設定鍵零變動。

#### 新功能
- **`DeluxeMenusImporter`**：純檔案層轉換器。讀 `<DeluxeMenus>/config.yml` 的 `gui_menus:` 取得 id→file 對應，把 `gui_menus/<file>` 的每個選單轉成 `java_menus/<id>.yml`。
  - 結構對應：`menu_title` 保留、`size: 54` → `rows: 6`、`items.<id>.{slot,material,display_name,lore,custom_model_data}` 原樣搬、`conditions`/`view_requirement`/`lock_message` 原樣保留供手動審閱、`update`/`skin` 略過。
  - 動作 prefix 對應：`[player]`/`[message]`/`[close]` 保留、`[console] X` → `[cmd] X`、`[openguimenu] X` → `[player] dgm open X`、未知 prefix 原樣保留。
  - 轉換後檔案頂部加 4 行中文註解，標註來源檔名 + prefix 對應 + 微調提示。
  - `sanitizeFileSegment` 防 path traversal：只保留 `[A-Za-z0-9._-]`、64 字元上限、拒絕 `.`/`..`/leading dots。
- **`LegacyImportService` 雙模式**：新增 `SourceType` enum 與 `IMPORT_CANDIDATES` 清單；`scan()` 自動偵測 DeluxeMenus 結構並標 `DELUXEMENUS`，其他舊版仍標 `LEGACY_MIAO`；新增 `previewDeluxeMenus` / `applyDeluxeMenus`（共用既有 backup / rotation / conflict 策略）。
- **`ImportCommand` UX 分流**：
  - `/dgm import scan` 顯示 DeluxeMenus 來源時用 `[DeluxeMenus]` 標籤
  - `/dgm import preview DeluxeMenus` 會先印多行轉換規則說明，再列出將生成的 `java_menus/<id>.yml`
  - `/dgm import apply` 偵測 session.sourceType 後分流到對應路徑
  - tab completion 與 findCandidate 改用 `IMPORT_CANDIDATES`（含 DeluxeMenus / dmenu）

#### 修正
- **作者標 Avery**：`plugin.yml` `author: MingLi` → `author: Avery11111101`；`AboutCommand` 拿掉 `about.original` 行、新增 `about.author` 行（固定 Avery11111101）。footer 加註「原作資訊請看 Fork 連結內的來源」，把原作引用收斂到 GitHub 的 fork 關係而非主畫面。
- **`help.commands.import.detail` 雙語改寫**：原本寫「DeluxeMenus 不在範圍」，現在改成兩段表格說明「同 schema 整批複製」vs「不同 schema 自動轉換」兩條路徑，並列出五個候選資料夾。
- **`import.scan.empty` 文案**：把候選列表從 3 個改回 5 個（加回 DeluxeMenus、dmenu）。

#### 翻譯漏洞檢查
- Python 比對 `Lang.get("…")` 共 122 個鍵（靜態 + 動態），zh_TW.yml 與 en.yml 皆齊全，漏鍵 0。
- 8 個「疑似純英文」是合理保留：指令名（`/dgm reload`）、權限節點（`dgeysermenu.use`）、品牌名（`MiaoMenu_fork`/`Fork`/`Modrinth`）。
- 真正沒翻的「DeluxeMenus 不在範圍」字串隨支援 DeluxeMenus 一併重寫。

#### 多代理 QA 循環
- Round 1：Scanner M（13 項全 PASS、findings=[]）+ Verifier N（10 個 use case 全 PASS）→ 雙簽 clean
- Round 2：Scanner O（12 項全 PASS、findings=[]）+ Verifier P（10 個 use case 全 PASS）→ 雙簽 clean
- 連續兩輪 clean、收工
- 對 Avery 的 `/Users/avery/Downloads/DeluxeMenus`（含 20 個選單 + 巢狀 `gui_menus/tutorial/*.yml`）跑 Python 模擬驗證：listMenus 20/20、`size: 54 → rows: 6`、`[openguimenu] world-tp → [player] dgm open world-tp` 等對應全綠

#### 維持不變
- 既有指令、別名、權限樹、`config-version=16`、`menu-version=6` 與所有設定鍵
- `LegacyDataMigrator.migrateIfNeeded`（靜默兜底）的候選清單仍是 3 項，DeluxeMenus 只走「手動 import」路徑（避免靜默轉換損壞使用者環境）
- `LegacyImportService.IMPORT_CANDIDATES`（手動 import）才包含 DeluxeMenus / dmenu

### `1.2` 增訂（2026-06-20，dmenu 修正 + LangCommand + AboutCommand + UpdateChecker + 詳解 help）

> Avery 連續三條指令收尾：每個指令要有 help 教學、要能用 `/lang` 切語言、要有 `/dgm about` + 啟動印資訊 + 管理員上線通知更新。實作中途澄清 `dmenu` 其實是 DeluxeMenus（不相容），順手一併修掉。本段為 1.2 的後續增訂，使用者既有指令／別名／權限／設定鍵零變動。

#### 新功能
- **每個子指令都有詳解 help**：`/dgm help <子指令>`（open / reload / import / lang / about / help 六項）會印出該指令的 usage、detail、permission。文案完全由 `lang/<language>.yml` 的 `help.commands.*` 區塊提供，切換語言後立即跟著變語系。
- **`/dgm lang [<語言碼>]` 切換顯示語言**：寫回 `config.yml` 的 `language` → `Lang.load(code)` → `reloadHelpDescriptions()` 熱替換 help 描述快取。無參數可查目前語言與可用清單。權限 `dgeysermenu.admin`。
- **`/mmflang <語言碼>`、`/lang <語言碼>` 短捷指令**：plugin.yml 新增獨立 `mmflang` 指令（alias `lang`），三種寫法都路由到同一個 `LangCommand`。⚠️ `/lang` 若與其他插件衝突（例如 LiteBans），Bukkit 依載入順序決定誰先註冊，使用者可改用 `/mmflang` 或 `/dgm lang` 作為穩定 fallback。
- **`/dgm about`**：顯示版本、Fork 與原作 GitHub、Modrinth 連結；若 Modrinth 有新版會在底部附提示。
- **啟動 banner**：onEnable 把同一份 about 資訊印到主控台，方便維運。
- **管理員上線通知更新**：`UpdateNoticeListener` 在 `PlayerJoinEvent` 用 `EventPriority.MONITOR` + `ignoreCancelled=true`，只給有 `dgeysermenu.admin` 權限的玩家發訊，且必須通過 `UpdateChecker.hasUpdate()` 雙重 guard，一般玩家不受打擾。

#### 修正
- **`LEGACY_CANDIDATES` 移除 `dmenu`**：原本以為 `dmenu` 是舊版 MiaoMenu 系列的一個資料夾，實際上是 DeluxeMenus（另一個完全不同的選單插件，YAML 結構不相容）。盲目自動匯入會弄壞使用者設定，故 1.2 起把 `dmenu` 從候選清單剔除，剩 `DGeyserMenu` / `dgeysermenu` / `MiaoMenu` 三項；`/dgm import scan` 的「無候選」訊息也同步說明 DeluxeMenus 不在範圍。

#### 安全 / 正確性
- **`UpdateChecker` 非同步且失敗不影響啟動**：用 `CompletableFuture.supplyAsync` + JDK 21 內建 `HttpClient`，8 秒 timeout；任何例外（網路、SSL、parser、interrupt）都吞掉並回 `null`；onEnable 完全不會因更新檢查而 block。
- **`LangCommand.sanitize()` 防 path traversal**：只保留 `[A-Za-z0-9_-]`，長度上限 32 字；最終的 `Files.exists(new File(dataFolder, "lang/<code>.yml"))` 不會跳脫 dataFolder。
- **`reloadHelpDescriptions()` 不重建子指令實例**：只替換 `HelpCommand` 內部的 `volatile Map<String,String>`，避免清掉 `ImportCommand` 等帶 session 狀態的子指令。
- **YAML 健全**：兩語系檔通過 Python `yaml.safe_load` 驗證，無重複頂層 key、無語法錯誤；24 個新增鍵在 zh_TW.yml 與 en.yml 一一對應。

#### 多代理 QA 循環（職責分離）
- Round 1：Scanner G + Verifier H 雙雙簽 clean
- Round 2：Scanner I clean、Verifier J 抓 1 minor（README 未提 `/lang` 衝突 trade-off）→ 主代理人補 README「切換語言」段含 ⚠️ 警示
- Round 3：Scanner K + Verifier L 雙雙簽 clean、findings=[]、confidence=high → **連續兩輪 clean、收工**
- 全程 `mvn -DskipTests clean package` BUILD SUCCESS

#### 維持不變
- 既有 `/dgm open|reload|import|help`、`/getmenuclock`、別名 `dgm/fluxmenu/mmf`、權限 `dgeysermenu.{use,admin,reload,import,*}` 與所有設定鍵
- `config-version=16`、`menu-version=6`、`config.yml`、`java_menus/*.yml`、`bedrock_menus/*.yml` 結構
- `LegacyDataMigrator.migrateIfNeeded` 的「首次啟動 + 無 config.yml 才觸發」靜默兜底語意，只是候選從 4 縮成 3

### `1.2`（2026-06-20，指令引導的舊版資料匯入）

> 補上「插件已運行的伺服器」無法手動補匯入舊資料的缺口。新增 `/dgm import` 系列指令，讓管理員在線上做 scan / preview / apply / rollback 完整流程，無需重啟伺服器。鐵則照舊：使用者既有指令／別名／權限／設定鍵零變動。本版完全是「附加」，不修改任何既有行為。

#### 新增功能
- **`/dgm import scan`**：掃描 `plugins/` 內可匯入的舊資料夾（`dmenu` / `DGeyserMenu` / `dgeysermenu` / `MiaoMenu`），列出 Java / Bedrock 選單數與 `config-version`。
- **`/dgm import preview <來源>`**：對比來源與當前 dataFolder，產生「新增 / 略過（內容相同）/ 衝突（內容不同）」三類差異，並產出 8 碼 SHA-1 確認碼（session TTL 5 分鐘）。
- **`/dgm import apply <來源> <確認碼> [--overwrite|--rename]`**：必須帶 `preview` 階段產生的確認碼才會落盤；衝突檔自動備份到 `plugins/MiaoMenu_fork/backups/import-<時戳>/`（保留最近 5 份，同秒衝突會加 `-2/-3` 後綴）；預設衝突策略為 `SKIP`，可改 `--overwrite` 直接覆寫或 `--rename` 加 `.imported` 後綴並存。
- **`/dgm import rollback [備份ID]`**：不帶參數＝最近一份；`backupId` 嚴格限制為 `[a-zA-Z0-9-]` 且須以 `import-` 開頭（path traversal 防守）。
- 套用 / 還原成功後自動 reload：`loadConfig() → JavaMenu loadAllMenus() → BedrockMenu loadAllMenus()`，順序與 `/dgm reload` 完全一致。

#### 安全 / 正確性
- **路徑守門**：`LegacyImportService.shouldIgnore` 拒絕含 `..` 或 `/` 開頭的相對路徑；`LegacyDataMigrator.copyDirectory`（首次啟動的兜底路徑）同樣補上相同防守。
- **執行緒安全**：session 用 `ConcurrentHashMap` + `compute()` 原子化 get/expire/remove，多管理員同時操作不會錯亂。
- **同秒備份衝突**：`createBackup` 偵測到時戳已存在時自動以 `-2/-3` 遞增後綴。
- **權限**：新增 `dgeysermenu.import`（預設 OP），同時掛在 `dgeysermenu.*` 的 children 下；`/dgm import` 也接受 `dgeysermenu.admin` 作為旁路。

#### 多代理 QA 循環
- Round 1：Scanner A 抓出 3 P2（path traversal、session race、同秒 backupId）+ 1 P3（既有 `getmenuclock` description 漏抽簡中）→ 主代理人套修 → Verifier B 簽 clean 並加碼指出 `countYamls` 理論 int 溢出 → 主代理人補 cap。
- Round 2：Verifier C 簽 clean；Verifier D 因 i18n 巢狀路徑未展開而誤判（鍵其實都在），但順手點出 `LegacyDataMigrator.copyDirectory` 也該補 path traversal → 主代理人查證 D 誤判 + 順手對齊兩條匯入路徑的守門。
- Round 3：Scanner E + Verifier F 雙雙簽 clean、`findings=[]`、`breaks_user_ops=false`、`confidence=high` → 連續兩輪 clean、收工。
- 三輪 `mvn -DskipTests clean package` 皆 BUILD SUCCESS。

#### 維持不變
- 既有 `/dgm open|reload|help`、`/getmenuclock`、別名 `dgm/fluxmenu/mmf`、權限 `dgeysermenu.{use,admin,reload,*}` 與所有設定鍵
- `config-version=16`、`menu-version=6`、`config.yml`、`java_menus/*.yml`、`bedrock_menus/*.yml` 結構
- `LegacyDataMigrator.migrateIfNeeded` 的「首次啟動 + 無 `config.yml` 才觸發」靜默兜底語意（兩條匯入路徑共用候選清單但行為各自獨立）

#### 順手清掉
- `plugin.yml` 的 `getmenuclock` description 由簡中「获取菜单钟表」改為繁中「取得選單時鐘」，與專案整體繁中語系一致

### `1.1`（2026-06-20，內部健檢循環）

> 「使用者操作零變動」前提下，做了第二輪「找問題 → 驗證嚴重性 → 修補 → 多代理交叉驗證」迴圈，連續兩位獨立驗證代理判 clean 才收尾。`config.yml`、`java_menus/*.yml`、`bedrock_menus/*.yml`、指令、權限、`config-version`、`menu-version` 全部不變。

#### 安全 / 正確性
- **權限條件補旁路**：`requirement` 寫 `type: permission` 但漏寫 `permission:` 欄位時，原本會放行 → 改為 fail-closed 直接拒絕，避免設定漏字導致玩家繞過權限。
- **`material` 容錯**：`java_menus/*.yml` 與 `config.yml` 的 `fallback-material` 若使用者寫成 YAML null（`~`）或空字串，原本會丟 `IllegalArgumentException` 卡住 onEnable / 開選單；改為自動退回 `STONE`。
- **未知 requirement type 不再靜默**：YAML 拼錯 `type:`（例如 `permssion`）原本會被默默跳過、玩家以為設了條件其實全沒套；改為印一次 warning（同 type 之後靜音，不洗版）。
- **`ScoreboardManager` null 防護**：早期啟動或 Folia 異常狀態下 `getScoreboardManager()` 可能傳 null；補上 null 檢查，避免條件評估時 NPE。
- **熱重載 watcher 緒清理**：`HotReloadManager.shutdown()` 改成「先關 `WatchService` → interrupt → `join(1000ms)`」順序，確保插件 disable 時 watcher 緒能正確結束。
- **`SoundsClock` 防護**：插件 disable 期間若仍有開選單流程觸發，包 try-catch 避免 `IllegalStateException` 中斷整個流程。
- **`saveResourceIfNotExists` 不再靜默**：jar 內找不到範例資源時原本直接吞例外，現在改 log warning。
- **`OpenCommand` tab 補全防護**：對 `args.length == 0` 與 `args[0] == null` 加邊界檢查。

#### 記憶體 / 資源
- **`RateLimiter` 加被動清理 + 主動移除**：每 60 秒 CAS 一次掃過期條目（超過 5×window）、玩家離線時 `PlayerQuitEvent` 主動移除 UUID，長期運轉伺服器不再緩慢累積條目。
- **`MenuClockManager.playerHasClock` 改用 `getContents()`**：原本只掃主物品欄漏掉副手，玩家把選單時鐘放副手後重連會被誤判為「沒時鐘」而重複給予；改用 `getContents()` 掃全部欄位。

#### PlaceholderAPI 一致性
- **鎖定訊息也吃得到 PlaceholderAPI**：原本 `MenuItem.resolveLockMessage` 傳 null plugin 給 `PlaceholderUtils.parse`，導致鎖定按鈕的 `deny_message` / `lock_message` 不會解析 `%placeholders%`；改為正確傳遞 `Plugin` 參數。
- **`JavaMenuListener` 單次評估**：點擊選單按鈕時原本 `isLocked()` 與 `getLockMessage()` 會各跑一次條件評估；新增 `LockState` record，一次評估同時取出 locked 狀態與鎖定訊息，避免重複計算且消除中間態不一致。

#### 解耦 / 可維護性
- **`RequirementService` 改吃 `Plugin` 介面**：建構式從 `RequirementService(MiaoMenu)` 改為 `RequirementService(Plugin)` + 測試友善的 `(Plugin, Server)` 雙建構式；`evaluateAdvancement` / `getScore` 改用注入的 `Server` 而非 `plugin.getServer()`，方便單元測試。
- **`JavaMenu` 改吃 `Plugin` 介面**：把硬綁的 `MiaoMenu plugin` 欄位替換為 `Plugin plugin`，降低耦合。
- **`MiaoMenu` 主類去掉 `final`**：plugin 主類無理由 final，去掉後便於將來測試與擴充。
- **`PlayerLifecycleListener` / `_Folia` onQuit null check**：保險起見對 `RateLimiter` 加 null 檢查，避免 onDisable 期間競態崩潰。

#### 驗證流程
- **多代理迴圈**：1 位掃描代理找出 20 條候選 → 主代理逐檔驗證得出 8 條真實／12 條誤判 → 套修補 → 驗證代理 A 找到 1 條漏網（PlaceholderAPI 沒傳 plugin）並補 → 驗證代理 B 對抗式找到 5 條 medium（修了 4 條，1 條 `BungeeCord` 偵測 M-4 延 v1.1.1）→ 驗證代理 A' 補 OpenCommand defensive → 驗證代理 C + Plan 角度驗證代理 D 連續兩次判 clean → 收工。
- **進度同步**：每輪結束都推 Discord webhook 通知 Avery。
- **`README.AI.md` 同步**：完整修補紀錄與動機已寫入 AI 上下文記憶庫。

#### 留尾項目（延 v1.1.1）
- **M-4**：`ProxyManager.detectProxyType` 用 `net.md_5.bungee.api.ChatColor` 類存在偵測 BungeeCord 不可靠（Paper / Spigot 自帶該類）；目前預設 `velocity-network: true` 走 Velocity 分支不出事，待重構偵測邏輯。
- `pom.xml` 改用 `<release>21</release>` 取代 `<source>` / `<target>`（屬「低嚴重度建議」，未動以縮小 PR）。

### `1.0`（2026-06-20，穩定首發）

- 經過 `0.1` → `0.2` 兩輪多代理掃描／修補／驗證迴圈後，所有 P0／P1 問題清零，正式升為穩定版本
- 版本號從 `0.x` 預備期跳升到 `1.0`，代表「使用者操作零變動」的相容承諾通過驗證
- 三語 README（繁中／英文／簡中）同步至本版，三份 README 都加入完整 changelog
- 產物改名為 `MiaoMenu_fork-1.0.jar`；本版未動程式邏輯，純文件、版本字串與打包編號變更，與 `0.2` 行為等價

### `0.2`（2026-06-20，穩定性與遷移）

- **無痛轉移**：新增 `LegacyDataMigrator`，首次啟動時自動從 `plugins/dmenu`、`plugins/DGeyserMenu`、`plugins/dgeysermenu`、`plugins/MiaoMenu` 匯入舊資料到本插件資料夾
- **修復：沒裝 Floodgate 時插件啟動失敗**：`BedrockMenuManager` 改為 lazy 反射初始化，純 Java 環境也能 onEnable
- **修復：`/getmenuclock` 指令未掛載**：補上 executor，含 `dgeysermenu.admin` 權限檢查與 Player 檢查
- **修復：`MENU_VERSION` 反向設定**：常數 3 → 6，與 `config.yml` 對齊，不再每次啟動覆寫使用者 `test.yml`
- **修復：Folia 環境玩家死亡會噴鐘**：`PlayerLifecycleListener_Folia` 補上 `onDeath` handler 過濾掉口袋鐘錶
- **修復：基岩選單 `requirement_blocks` 共用條件失效**：點擊處理改傳真實 blocks map，而非 `Collections.emptyMap()`
- **安全：阻擋潛在 console 命令注入**：`CmdAction` 接入 `InputValidator.isSafeCommandContent` 過濾，注入內容直接拒絕並回送 `unsafe-input` 訊息
- **體驗：時鐘右鍵不再雙手雙觸發**：`ClockInteractionListener` 加入 `EquipmentSlot.HAND` 早退
- **`plugin.yml` softdepend 大小寫修正**：`Floodgate` → `floodgate`，與其實際 plugin name 對齊
- **多語系：`menu.locked-tag` 抽出**：基岩鎖定按鈕的「[未解鎖]」標籤改由 `lang/<language>.yml` 控制

### `0.1`（2026-06-20，Fork 起點）

- 跟上 MC 26.x：`paper-api` 1.21.11 → **26.1.2.build.72-stable**、`folia-api` → 26.1.2.build.8-stable
- 升級 `floodgate` 2.2.0 → 2.2.5-SNAPSHOT、新增 `geyser` 2.10.1-SNAPSHOT 軟相依
- `plugin.yml` 的 `api-version` 由 `1.21` 改為 `'26.1'`
- 多語系拆分：原 `config.yml` 內 `messages:` 區塊抽出為 `lang/<language>.yml`，內建 `en`（預設）與 `zh_TW`
- `Lang.load()` 查詢順序：`lang/<language>.yml` → `lang/en.yml` fallback → `config.yml`（向後相容舊用法）
- 熱重載延伸：`HotReloadManager` 監聽 `lang/` 目錄，編輯語系檔即時生效
- 指令別名新增 `/mmf`，不影響原本 `/dgm`、`/fluxmenu`、`/dgeysermenu`
- 改名 `MiaoMenu_fork`：jar 與插件資料夾改名為 `MiaoMenu_fork`；原本指令、權限節點、設定鍵名「完全沒動」
- 中文行文重整：README、設定檔 header、lang 訊息一律改稱「插件」，去掉重複的「（台灣）」尾綴

---

## English

### `1.2` follow-up #2 (2026-06-20, DeluxeMenus auto-converting import + translation audit + author = Avery)

> Three more follow-ups from Avery after the first 1.2 follow-up: "DeluxeMenus import should be supported", "some help pages are still untranslated to Chinese", and "for the author, only write me — the upstream is referenced on GitHub already". After reading the real DeluxeMenus folder we re-added DeluxeMenus/dmenu to the import candidates, but with proper schema conversion (not a verbatim copy). Existing commands / aliases / permissions / config keys are untouched.

**New**
- **`DeluxeMenusImporter`** — pure file-layer converter. Reads `<DeluxeMenus>/config.yml`'s `gui_menus:` for the id→file mapping, then converts each `gui_menus/<file>` into `java_menus/<id>.yml`.
  - Field mapping: `menu_title` preserved; `size: 54` → `rows: 6`; `items.<id>.{slot, material, display_name, lore, custom_model_data}` carried verbatim; `conditions` / `view_requirement` / `lock_message` preserved for manual review; `update` / `skin` dropped (no MiaoMenu equivalent).
  - Action prefix mapping: `[player]` / `[message]` / `[close]` preserved; `[console] X` → `[cmd] X`; `[openguimenu] X` → `[player] dgm open X`; unknown prefixes are preserved verbatim.
  - Output gets a 4-line Chinese header comment noting source file, prefix mapping and PlaceholderAPI tweak hint.
  - `sanitizeFileSegment` blocks path traversal: only `[A-Za-z0-9._-]` allowed, capped at 64 chars, rejects `.` / `..` / leading dots.
- **`LegacyImportService` dual mode** — new `SourceType` enum + `IMPORT_CANDIDATES` list; `scan()` auto-detects DeluxeMenus structure and tags it `DELUXEMENUS`, others stay `LEGACY_MIAO`; new `previewDeluxeMenus` / `applyDeluxeMenus` share the existing backup / rotation / conflict strategy.
- **`ImportCommand` UX routing** —
  - `/dgm import scan` tags DeluxeMenus sources with `[DeluxeMenus]`
  - `/dgm import preview DeluxeMenus` prints the conversion rules note first, then lists the would-be `java_menus/<id>.yml` files
  - `/dgm import apply` routes by `session.sourceType` to the matching path
  - Tab completion + findCandidate now use `IMPORT_CANDIDATES` (includes DeluxeMenus / dmenu)

**Fix**
- **Author tagged Avery** — `plugin.yml` `author: MingLi` → `author: Avery11111101`; `AboutCommand` drops the `about.original` line and adds an `about.author` line (fixed to Avery11111101). The footer now reads "see the Fork link for upstream attribution" — keeping the upstream reference where GitHub already exposes it (the fork relationship).
- **`help.commands.import.detail` rewritten in both languages** — used to say "DeluxeMenus is excluded"; now shows a two-section table explaining "same-schema verbatim copy" vs "schema-converting import" and lists all five candidate folders.
- **`import.scan.empty` text** — candidate list goes from 3 back to 5 (re-adds DeluxeMenus, dmenu).

**Translation audit**
- Python cross-check against all `Lang.get("...")` call sites — 122 keys (static + dynamic) all present in both zh_TW.yml and en.yml, missing count: 0.
- The 8 "looks like English" strings in zh_TW.yml are legitimately preserved: command names (`/dgm reload`), permission nodes (`dgeysermenu.use`), brand names (`MiaoMenu_fork` / `Fork` / `Modrinth`).
- The actual untranslated string ("DeluxeMenus is excluded") was rewritten as part of adding DeluxeMenus support.

**Multi-agent QA loop**
- Round 1: Scanner M (13/13 checks PASS, findings=[]) + Verifier N (10/10 use cases PASS) → both signed off clean.
- Round 2: Scanner O (12/12 checks PASS, findings=[]) + Verifier P (10/10 use cases PASS) → both signed off clean.
- Two consecutive clean rounds — loop exits.
- Python simulation against Avery's real `/Users/avery/Downloads/DeluxeMenus` (20 menus including nested `gui_menus/tutorial/*.yml`) — listMenus 20/20, `size: 54 → rows: 6`, `[openguimenu] world-tp → [player] dgm open world-tp`, etc., all green.

**Unchanged**
- Existing commands, aliases, permissions, `config-version=16`, `menu-version=6` and every existing config key
- `LegacyDataMigrator.migrateIfNeeded` (silent first-boot fallback) candidate list stays at 3 — DeluxeMenus only goes through the manual `/dgm import` path so silent conversion never breaks a user's environment
- `LegacyImportService.IMPORT_CANDIDATES` (manual import) is the one that includes DeluxeMenus / dmenu

### `1.2` follow-up (2026-06-20, dmenu fix + LangCommand + AboutCommand + UpdateChecker + per-command help)

> Three back-to-back follow-up requests from Avery: every command needs a help walkthrough, add `/lang` for live language switching, add `/dgm about` plus a startup banner and an admin-on-join update notice. Mid-implementation Avery clarified that `dmenu` is actually DeluxeMenus (different YAML schema) — fixed alongside. This is the 1.2 follow-up; existing commands / aliases / permissions / config keys are untouched.

**New features**
- **Per-subcommand deep dive**: `/dgm help <sub>` (open / reload / import / lang / about / help) prints that command's usage, detail and permission. Sourced from `lang/<language>.yml`'s `help.commands.*` block, so the deep dive switches language with `/dgm lang` instantly.
- **`/dgm lang [<code>]`**: writes config.yml's `language` key → `Lang.load(code)` → `reloadHelpDescriptions()` swaps the help description cache. With no arg, shows current + available codes. Permission: `dgeysermenu.admin`.
- **`/mmflang <code>` and `/lang <code>` shortcuts**: plugin.yml registers a standalone `mmflang` command (alias `lang`), all three forms route to the same `LangCommand`. ⚠️ `/lang` collides with other plugins (e.g. LiteBans); Bukkit resolves by load order — fall back to `/mmflang` or `/dgm lang` for a stable form.
- **`/dgm about`**: version + Fork & Original GitHub + Modrinth links. If a newer version is detected on Modrinth, a download hint is appended.
- **Startup banner**: onEnable prints the same about info to console for ops visibility.
- **Admin-on-join update notice**: `UpdateNoticeListener` uses `EventPriority.MONITOR` + `ignoreCancelled=true`, only messages players with `dgeysermenu.admin`, gated by `UpdateChecker.hasUpdate()`. Regular players are not bothered.

**Fix**
- **`LEGACY_CANDIDATES` drops `dmenu`**: `dmenu` is DeluxeMenus's plugin folder (a completely different menu plugin with an incompatible YAML schema). Auto-importing it would break user configs, so 1.2 removes it from the candidate list — leaving `DGeyserMenu` / `dgeysermenu` / `MiaoMenu`. The `/dgm import scan` empty-state message also calls out that DeluxeMenus is excluded.

**Security / correctness**
- **`UpdateChecker` is non-blocking and failure-safe**: `CompletableFuture.supplyAsync` + JDK 21 `HttpClient` + 8s timeout; any exception (network, SSL, parser, interrupt) is swallowed and returns null. onEnable never blocks on the check.
- **`LangCommand.sanitize()` path-traversal guard**: keeps only `[A-Za-z0-9_-]`, capped at 32 chars; the final `Files.exists(...)` cannot escape the data folder.
- **`reloadHelpDescriptions()` doesn't rebuild subcommand instances**: it swaps `HelpCommand`'s `volatile Map<String,String>` only, so `ImportCommand` session state survives a language switch.
- **YAML integrity**: both lang files pass `yaml.safe_load`; no duplicate top-level keys; 24 new keys mirror exactly across `zh_TW.yml` and `en.yml`.

**Multi-agent QA loop**
- Round 1: Scanner G + Verifier H both signed off clean.
- Round 2: Scanner I clean; Verifier J flagged 1 minor (README didn't mention the `/lang` collision trade-off) → main agent added a "Language switching" section to README with a ⚠️ note.
- Round 3: Scanner K + Verifier L both signed off `clean`, `findings: []`, `confidence: high` → two consecutive clean rounds, loop exits.
- All `mvn -DskipTests clean package` runs were BUILD SUCCESS.

**Unchanged**
- Existing `/dgm open|reload|import|help`, `/getmenuclock`, aliases `dgm/fluxmenu/mmf`, permissions `dgeysermenu.{use,admin,reload,import,*}` and every existing config key
- `config-version=16`, `menu-version=6`, `config.yml`, `java_menus/*.yml`, `bedrock_menus/*.yml` schemas
- `LegacyDataMigrator.migrateIfNeeded`'s "first-boot only, only when `config.yml` is absent" silent-fallback semantics — candidates shrink from 4 to 3 but the flow is identical

### `1.2` (2026-06-20, command-driven legacy menu import)

> Fills the gap that an already-running server couldn't manually re-import legacy menu data. Adds `/dgm import scan|preview|apply|rollback` so admins can do the whole flow online without restarting. Same "zero user-facing change" contract as before — purely additive, no existing behaviour is modified.

**New commands**
- **`/dgm import scan`** — scans `plugins/` for legacy candidate folders (`dmenu` / `DGeyserMenu` / `dgeysermenu` / `MiaoMenu`) and lists Java / Bedrock menu counts and `config-version`.
- **`/dgm import preview <source>`** — computes a diff against the current data folder bucketed as add / skip-same / conflict, and emits an 8-char SHA-1 confirm token (session TTL 5 min).
- **`/dgm import apply <source> <token> [--overwrite|--rename]`** — refuses to write without the token from `preview`. Conflicting files are first copied to `plugins/MiaoMenu_fork/backups/import-<timestamp>/` (retains the latest 5; suffixes `-2/-3` on same-second collision). Default conflict policy is `SKIP`; `--overwrite` overwrites in place, `--rename` keeps the existing file and writes the legacy one with `.imported`.
- **`/dgm import rollback [backupId]`** — no arg = latest backup; `backupId` is restricted to `[a-zA-Z0-9-]` and must start with `import-` (path traversal guard).
- On successful apply / rollback the plugin auto-reloads: `loadConfig() → JavaMenu loadAllMenus() → BedrockMenu loadAllMenus()` — exact same order as `/dgm reload`.

**Security / correctness**
- **Path guard.** `LegacyImportService.shouldIgnore` rejects relative paths containing `..` or starting with `/`; `LegacyDataMigrator.copyDirectory` (the first-boot silent fallback path) gets the same guard, aligning both routes.
- **Thread safety.** Sessions live in a `ConcurrentHashMap` and the get / expire / remove sequence runs atomically via `compute()`, so concurrent admins don't trample each other.
- **Same-second backup collision.** `createBackup` detects an existing directory and appends `-2/-3...` until unique.
- **Permission.** Adds `dgeysermenu.import` (default `op`), wired under the `dgeysermenu.*` aggregate. `/dgm import` also accepts `dgeysermenu.admin` as a bypass.

**Multi-agent QA loop**
- Round 1: Scanner A found 3 P2s (path traversal, session race, same-second backupId) plus 1 P3 (legacy simplified-Chinese `getmenuclock` description) → main agent patched → Verifier B signed off clean and flagged a theoretical `int` overflow in `countYamls` → main agent added a cap.
- Round 2: Verifier C signed off clean; Verifier D produced an i18n false-positive (didn't expand the nested YAML path) but legitimately pointed out that `LegacyDataMigrator.copyDirectory` should mirror the path-traversal guard → main agent verified the false-positive and aligned both paths.
- Round 3: Scanner E and Verifier F both signed off `clean`, `findings: []`, `breaks_user_ops: false`, `confidence: high` → two consecutive clean rounds, loop exits.
- All three `mvn -DskipTests clean package` runs were BUILD SUCCESS.

**Unchanged**
- Existing `/dgm open|reload|help`, `/getmenuclock`, aliases `dgm/fluxmenu/mmf`, permissions `dgeysermenu.{use,admin,reload,*}` and every existing config key
- `config-version=16`, `menu-version=6`, `config.yml`, `java_menus/*.yml`, `bedrock_menus/*.yml` schemas
- `LegacyDataMigrator.migrateIfNeeded`'s "first-boot only, only when `config.yml` is absent" silent-fallback semantics (both import paths share the candidate list, but their flows stay independent)

**Drive-by cleanup**
- `plugin.yml` `getmenuclock` description switched from simplified Chinese "获取菜单钟表" to Traditional Chinese "取得選單時鐘", aligning with the project-wide locale.

### `1.1` (2026-06-20, internal health-check loop)

> Same "zero user-facing change" contract as `1.0`. A second multi-agent **scan → severity-validate → fix → cross-verify** loop was run, with two independent verifier agents both signing off clean before the release. `config.yml`, menu YAMLs, commands, permissions, `config-version` and `menu-version` are all unchanged.

**Security / correctness**
- **Permission requirement is fail-closed.** Writing `type: permission` without a `permission:` key used to allow players through; it now denies, eliminating an accidental-bypass class of bug.
- **`material` null-tolerant.** YAML `material: ~` or empty string in `java_menus/` (and `fallback-material` in `config.yml`) used to throw `IllegalArgumentException` and kill onEnable / menu open; now falls back to `STONE`.
- **Unknown requirement `type` no longer silent.** A typo like `type: permssion` used to be silently skipped, leaving conditions unenforced; now logs a one-shot warning per type (further occurrences suppressed).
- **`ScoreboardManager` null guard.** During early startup or under abnormal Folia states `getScoreboardManager()` can return null; checked before use to avoid NPE.
- **Hot-reload watcher shutdown order.** `HotReloadManager.shutdown()` now closes the `WatchService` first, then interrupts and `join(1000ms)`s the watcher thread to ensure clean termination on disable.
- **`SoundsClock` resilience.** Wrapped in try-catch + `plugin.isEnabled()` so a disable-time menu open won't crash the flow with `IllegalStateException`.
- **`saveResourceIfNotExists` no longer silent.** When a packaged sample is missing from the jar, the failure is now warned instead of swallowed.
- **`OpenCommand` tab-complete defensive.** Guards against `args.length == 0` and `args[0] == null`.

**Memory / resources**
- **`RateLimiter` lazy sweep + proactive removal.** A CAS-guarded sweep every 60s drops entries older than 5×window, and `PlayerQuitEvent` proactively removes the UUID — preventing slow memory growth on long-running servers.
- **`MenuClockManager.playerHasClock` scans full inventory.** Switched from `getStorageContents()` (main inv only) to `getContents()` so a menu clock stashed in the off-hand isn't mistaken for "missing" and duplicated on the next join.

**PlaceholderAPI consistency**
- **Lock messages now resolve PlaceholderAPI.** `MenuItem.resolveLockMessage` used to pass `null` as the plugin to `PlaceholderUtils.parse`, so `%placeholders%` in `deny_message` / `lock_message` weren't substituted; the plugin reference is now correctly threaded through.
- **`JavaMenuListener` single-evaluate.** A new `LockState` record exposes locked-flag + lock-message in one call, replacing two separate evaluations and eliminating any in-between inconsistency.

**Decoupling / maintainability**
- **`RequirementService` takes `Plugin`.** Constructor changed from `RequirementService(MiaoMenu)` to `RequirementService(Plugin)` + a test-friendly `(Plugin, Server)` overload. `evaluateAdvancement` and `getScore` use the injected `Server` directly.
- **`JavaMenu` takes `Plugin`.** Hard-coded `MiaoMenu` field replaced with `Plugin` for looser coupling.
- **`MiaoMenu` main class is no longer `final`.** No semantic reason for it to be final; removing it makes testing and future extension cleaner.
- **`PlayerLifecycleListener` / `_Folia` onQuit null guard.** Defensive null check on `RateLimiter` in case quit fires during a partial onDisable.

**Verification flow**
- 1 scanner found 20 candidates → main agent validated → 8 real / 12 false-positives → fixes applied → verifier A caught 1 missing thread (PlaceholderAPI plugin) → patched → adversarial verifier B caught 5 mediums (4 fixed; 1 `BungeeCord` detection M-4 deferred to v1.1.1) → verifier A' caught `OpenCommand` defensive → patched → verifier C and Plan-style verifier D both signed off clean for two consecutive rounds → released.
- Progress was streamed to Discord webhook each round.
- `README.AI.md` updated with the full motivation / step trail.

**Deferred to v1.1.1**
- **M-4**: `ProxyManager.detectProxyType` uses `net.md_5.bungee.api.ChatColor` class presence to detect BungeeCord — unreliable because Paper / Spigot ship that class. The default `velocity-network: true` path masks this; detection logic needs rewriting.
- `pom.xml` should switch to `<release>21</release>` instead of `<source>` / `<target>` (low severity).

### `1.0` (2026-06-20, stable release)

- After two rounds of multi-agent scan / fix / verify loops (`0.1` → `0.2`), all P0 / P1 findings are clean — promoted to stable.
- Version jumps from the `0.x` preview range to `1.0` to signal that the "zero user-facing change" compatibility contract has been validated.
- Trilingual READMEs (Traditional Chinese / English / Simplified Chinese) are aligned to this version with the full changelog.
- Build artefact renamed to `MiaoMenu_fork-1.0.jar`. No code logic changed in this release — docs, version strings and packaging numbers only; runtime behaviour is equivalent to `0.2`.

### `0.2` (2026-06-20, stability & migration)

- **Drop-in migration**: new `LegacyDataMigrator` imports from `plugins/dmenu`, `plugins/DGeyserMenu`, `plugins/dgeysermenu`, `plugins/MiaoMenu` on first start.
- **Fix**: plugin no longer fails to enable when Floodgate is absent — `BedrockMenuManager` reflection is lazy.
- **Fix**: `/getmenuclock` executor was missing; now wired with `dgeysermenu.admin` and Player check.
- **Fix**: `MENU_VERSION` constant corrected (3 → 6) — sample `test.yml` is no longer overwritten on every start.
- **Fix (Folia)**: `PlayerLifecycleListener_Folia` adds `onDeath` so the menu clock no longer drops on death.
- **Fix**: Bedrock `requirement_blocks` shared conditions now resolved (real blocks map passed instead of empty).
- **Security**: `CmdAction` now passes through `InputValidator.isSafeCommandContent`; injected content is rejected with an `unsafe-input` message.
- **UX**: clock right-click no longer double-fires (off-hand event is dropped).
- **plugin.yml**: `softdepend` casing fixed (`Floodgate` → `floodgate`).
- **i18n**: `menu.locked-tag` extracted so the Bedrock locked-button tag is translatable.

### `0.1` (2026-06-20, fork start)

- Bumped to MC 26.x: `paper-api` → 26.1.2.build.72-stable, `folia-api` → 26.1.2.build.8-stable.
- `floodgate` 2.2.0 → 2.2.5-SNAPSHOT; added `geyser` 2.10.1-SNAPSHOT soft-dep.
- `plugin.yml` `api-version` → `'26.1'`.
- i18n split: `messages:` block in `config.yml` moved to `lang/<language>.yml`; `en` and `zh_TW` shipped.
- `Lang.load()` lookup order: `lang/<language>.yml` → `lang/en.yml` fallback → `config.yml` (backward compatible).
- Hot-reload now watches `lang/` too.
- New `/mmf` alias; `/dgm`, `/fluxmenu`, `/dgeysermenu` untouched.
- Renamed to `MiaoMenu_fork`: jar + plugin folder renamed; commands / permissions / config keys unchanged.
